
language: java
jdk:
  - oraclejdk8

script:
  - "cd website"
  - "mvn clean"
  - "mvn cobertura:cobertura"
  - "mvn package -Dmaven.test.skip=true"
branches:
  only:
    - master 

    
after_success:
- bash <(curl -s https://codecov.io/bash)